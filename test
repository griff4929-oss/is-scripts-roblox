-- =========================
-- GRIFF HUB v9 - ORION FIXED
-- =========================

-- Cargar Orion Library
local OrionLib = loadstring(game:HttpGet('https://raw.githubusercontent.com/jensonhirst/Orion/main/source'))()

-- Servicios
local RS = game:GetService("ReplicatedStorage")
local WS = game:GetService("Workspace")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Lighting = game:GetService("Lighting")

-- Variables
local nightmareActive = false
local crazyActive = false
local currentWaitTime = 16
local currentWaitTime2 = 11
local minWaitTime = 5
local farmTasks = {}

-- =========================
-- SISTEMA DE GUARDADO MANUAL
-- =========================

local SaveFolder = "GriffHub"
local SaveFile = SaveFolder .. "/Config.json"

local function saveState()
    if not writefile then return end
    if not isfolder(SaveFolder) then makefolder(SaveFolder) end
    
    local data = {
        nightmareActive = nightmareActive,
        crazyActive = crazyActive
    }
    local success, encoded = pcall(function()
        return game:GetService("HttpService"):JSONEncode(data)
    end)
    if success then
        pcall(function() writefile(SaveFile, encoded) end)
    end
end

local function loadState()
    if not readfile then return end
    local success, content = pcall(function() return readfile(SaveFile) end)
    if success and content and content ~= "" then
        local success2, data = pcall(function()
            return game:GetService("HttpService"):JSONDecode(content)
        end)
        if success2 and data then
            nightmareActive = data.nightmareActive or false
            crazyActive = data.crazyActive or false
        end
    end
end

loadState()

-- =========================
-- ANTI AFK
-- =========================
local vu = game:GetService("VirtualUser")
game:GetService("Players").LocalPlayer.Idled:Connect(function()
    vu:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    task.wait(1)
    vu:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
end)

-- =========================
-- FUNCIONES DE AUTOFARM
-- =========================

local function mejorarUnidad(nombreUnidad)
    local functions = RS:FindFirstChild("Functions")
    local spawnTower = functions and functions:FindFirstChild("SpawnTower")
    if not spawnTower then return end
    
    local towers = WS:FindFirstChild("Towers")
    if not towers then return end
    
    for _, torre in pairs(towers:GetChildren()) do
        local config = torre:FindFirstChild("Config")
        if config and config:FindFirstChild("Owner") and config.Owner.Value == LocalPlayer.Name then
            local nombre = config:FindFirstChild("TowerName")
            local upgrade = config:FindFirstChild("Upgrade")
            if nombre and nombre.Value == nombreUnidad and upgrade and upgrade.Value then
                pcall(function()
                    spawnTower:InvokeServer(upgrade.Value.Name, torre.PrimaryPart.CFrame, torre)
                end)
            end
        end
    end
end

local function venderTodosLosBunnies()
    local sellTower = RS.Events:FindFirstChild("SellTower") or RS.Functions:FindFirstChild("SellTower")
    if not sellTower then return 0 end
    
    local towers = WS:FindFirstChild("Towers")
    if not towers then return 0 end
    
    local vendidos = 0
    for _, tower in pairs(towers:GetChildren()) do
        local config = tower:FindFirstChild("Config")
        if config and config:FindFirstChild("Owner") and config.Owner.Value == LocalPlayer.Name then
            local tName = config:FindFirstChild("TowerName")
            if tName and (tName.Value == "Large Bunny TV Man" or tName.Value == "Bunny TV Man") then
                pcall(function() sellTower:InvokeServer(tower) end)
                vendidos = vendidos + 1
            end
        end
    end
    return vendidos
end

-- =========================
-- NIGHTMARE
-- =========================

local function stopNightmare()
    nightmareActive = false
    saveState()
    if farmTasks.nightmare then
        task.cancel(farmTasks.nightmare)
        farmTasks.nightmare = nil
    end
    print("❌ Nightmare desactivado")
end

local function startNightmare()
    if nightmareActive then return end
    nightmareActive = true
    saveState()
    
    farmTasks.nightmare = task.spawn(function()
        local argsEnter = { [1] = "Elevator16" }
        local argsStart = { [1] = "Elevator16" }
        
        pcall(function() 
            RS.Events.EnterElevator:FireServer(unpack(argsEnter))
        end)
        pcall(function() 
            RS.Events.StartElevator:FireServer(unpack(argsStart))
        end)
        
        if not nightmareActive then return end
        
        local bunnyCFrame = CFrame.new(-25.735321, 86.0418549, -85.5302887)
        local titanCFrame = CFrame.new(-47.0388031, 90.3418045, -86.8553925)
        
        task.spawn(function()
            while nightmareActive do
                pcall(function() RS.Events.VoteSkip:FireServer() end)
                task.wait(0.05)
            end
        end)
        
        task.spawn(function()
            while nightmareActive do
                mejorarUnidad("Large Bunny TV Man")
                mejorarUnidad("Upgraded Titan Cameraman")
                task.wait(0.3)
            end
        end)
        
        currentWaitTime = 16
        local bunnyStartTime = tick()
        
        while nightmareActive and (tick() - bunnyStartTime) < 60 do
            local waited = 0
            while nightmareActive and waited < currentWaitTime and (tick() - bunnyStartTime) < 60 do
                task.wait(1)
                waited = waited + 1
            end
            
            if nightmareActive and (tick() - bunnyStartTime) < 60 then
                pcall(function() RS.Functions.SpawnTower:InvokeServer("Large Bunny TV Man", bunnyCFrame) end)
                if currentWaitTime > minWaitTime then
                    currentWaitTime = math.max(minWaitTime, currentWaitTime - 1)
                end
            end
        end
        
        if not nightmareActive then return end
        
        currentWaitTime = 16
        
        while nightmareActive do
            local waited = 0
            while nightmareActive and waited < currentWaitTime do
                task.wait(1)
                waited = waited + 1
            end
            
            if nightmareActive then
                pcall(function() RS.Functions.SpawnTower:InvokeServer("Upgraded Titan Cameraman", titanCFrame) end)
                if currentWaitTime > minWaitTime then
                    currentWaitTime = math.max(minWaitTime, currentWaitTime - 1)
                end
            end
        end
    end)
end

-- =========================
-- CRAZY
-- =========================

local function stopCrazy()
    crazyActive = false
    saveState()
    if farmTasks.crazy then
        task.cancel(farmTasks.crazy)
        farmTasks.crazy = nil
    end
    print("❌ Crazy desactivado")
end

local function startCrazy()
    if crazyActive then return end
    crazyActive = true
    saveState()
    
    farmTasks.crazy = task.spawn(function()
        local argsEnter = { [1] = "Elevator19" }
        local argsStart = { [1] = "Elevator19" }
        
        pcall(function() 
            RS.Events.EnterElevator:FireServer(unpack(argsEnter))
        end)
        pcall(function() 
            RS.Events.StartElevator:FireServer(unpack(argsStart))
        end)
        
        if not crazyActive then return end
        
        local scientistCFrame = CFrame.new(41.5543137, 82.593399, -44.0053787)
        local bunnyCFrame2 = CFrame.new(29.8349686, 84.2882462, -17.7190742)
        local mechCFrame2 = CFrame.new(7.5827179, 88.5881958, -28.159481)
        local helicopterCFrame = CFrame.new(12.570381164550781, 85.38301086425781, -0.2645606994628906)
        
        for i = 1, 4 do
            pcall(function() RS.Functions.SpawnTower:InvokeServer("Scientist Cameraman", scientistCFrame) end)
            task.wait(0.3)
        end
        
        task.spawn(function()
            while crazyActive do
                pcall(function() RS.Events.VoteSkip:FireServer() end)
                task.wait(0.05)
            end
        end)
        
        task.spawn(function()
            while crazyActive do
                mejorarUnidad("Large Bunny TV Man")
                mejorarUnidad("Scientist Cameraman")
                mejorarUnidad("Mech Cameraman")
                mejorarUnidad("Large Speaker Helicopter")
                task.wait(0.3)
            end
        end)
        
        currentWaitTime2 = 11
        local bunnyStartTime = tick()
        
        while crazyActive and (tick() - bunnyStartTime) < 60 do
            local waited = 0
            while crazyActive and waited < currentWaitTime2 and (tick() - bunnyStartTime) < 60 do
                task.wait(1)
                waited = waited + 1
            end
            
            if crazyActive and (tick() - bunnyStartTime) < 60 then
                pcall(function() RS.Functions.SpawnTower:InvokeServer("Large Bunny TV Man", bunnyCFrame2) end)
                if currentWaitTime2 > minWaitTime then
                    currentWaitTime2 = math.max(minWaitTime, currentWaitTime2 - 1)
                end
            end
        end
        
        if not crazyActive then return end
        
        local mechStartTime = tick()
        
        while crazyActive and (tick() - mechStartTime) < 50 do
            local waited = 0
            while crazyActive and waited < 3 and (tick() - mechStartTime) < 50 do
                task.wait(1)
                waited = waited + 1
            end
            
            if crazyActive and (tick() - mechStartTime) < 50 then
                pcall(function() RS.Functions.SpawnTower:InvokeServer("Mech Cameraman", mechCFrame2) end)
            end
        end
        
        if not crazyActive then return end
        
        venderTodosLosBunnies()
        task.wait(1)
        
        if not crazyActive then return end
        
        while crazyActive do
            local waited = 0
            while crazyActive and waited < 10 do
                task.wait(1)
                waited = waited + 1
            end
            
            if crazyActive then
                pcall(function() RS.Functions.SpawnTower:InvokeServer("Large Speaker Helicopter", helicopterCFrame) end)
            end
        end
    end)
end

-- =========================
-- CREAR INTERFAZ CON ORION (CON TOGGLES FIXEADOS)
-- =========================

local Window = OrionLib:MakeWindow({
    Name = "🥚 Griff Hub v9",
    HidePremium = false,
    SaveConfig = false,
    ConfigFolder = "GriffHub",
    IntroEnabled = false
})

-- Pestaña AutoFarm
local FarmTab = Window:MakeTab({
    Name = "🌾 AutoFarm",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Sección Nightmare
FarmTab:AddSection({
    Name = "🌙 Nightmare"
})

-- Toggle con fix para Orion
local nightmareToggle = FarmTab:AddToggle({
    Name = "Activar Nightmare",
    Default = nightmareActive,
    Callback = function(Value)
        -- Forzar ejecución con task.spawn para evitar bugs de Orion
        task.spawn(function()
            if Value then
                startNightmare()
            else
                stopNightmare()
            end
        end)
    end
})

-- Sección Crazy
FarmTab:AddSection({
    Name = "🌀 Crazy"
})

local crazyToggle = FarmTab:AddToggle({
    Name = "Activar Crazy",
    Default = crazyActive,
    Callback = function(Value)
        task.spawn(function()
            if Value then
                startCrazy()
            else
                stopCrazy()
            end
        end)
    end
})

-- Pestaña Optimizar
local OptimizeTab = Window:MakeTab({
    Name = "⚡ Optimizar",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

OptimizeTab:AddSection({
    Name = "Optimizaciones"
})

OptimizeTab:AddButton({
    Name = "⚡ Boost FPS",
    Callback = function()
        task.spawn(function()
            pcall(function()
                Lighting.GlobalShadows = false
                Lighting.FogEnd = 1e10
                Lighting.Brightness = 1
                settings().Rendering.QualityLevel = 1
            end)
            OrionLib:MakeNotification({
                Name = "FPS Boost",
                Content = "Configuración gráfica optimizada",
                Image = "rbxassetid://4483345998",
                Time = 3
            })
        end)
    end
})

OptimizeTab:AddButton({
    Name = "🔄 Rejoin",
    Callback = function()
        task.spawn(function()
            OrionLib:MakeNotification({
                Name = "Rejoin",
                Content = "Reconectando al juego...",
                Image = "rbxassetid://4483345998",
                Time = 2
            })
            task.wait(1)
            game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer)
        end)
    end
})

-- Notificación de inicio
OrionLib:MakeNotification({
    Name = "Griff Hub v9",
    Content = "¡Interfaz cargada! Los toggles ahora funcionan correctamente.",
    Image = "rbxassetid://4483345998",
    Time = 5
})

print("✅ Griff Hub v9 - Versión con toggles fixeados")
